import AdminDashboardClient from "@/components/AdminDashboardClient";

export const metadata = { title: "Admin — Finora" };

export default function AdminPage() {
  return <AdminDashboardClient />;
}
