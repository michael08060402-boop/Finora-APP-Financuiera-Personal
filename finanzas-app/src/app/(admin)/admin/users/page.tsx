import AdminUsersClient from "@/components/AdminUsersClient";

export const metadata = { title: "Usuarios — Admin Finora" };

export default function AdminUsersPage() {
  return <AdminUsersClient />;
}
